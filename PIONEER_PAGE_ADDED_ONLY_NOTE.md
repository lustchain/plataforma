# LUST Pioneer Rewards page

The existing site files were left unchanged. This package only adds:

- `pioneer.html`
- `pioneer.css`
- `pioneer.js`
- `pioneer-config.js`
- `assets/pioneer/*.png`
- `pioneer-server/` deployment package

Direct page URL after upload:

`https://platform.lustchain.org/pioneer.html`

Before public launch, edit only `pioneer-config.js` to add:

- `officialXProfileUrl`
- `officialCampaignPostUrl`

The contract address is already configured as:

`0xCb207489E4dbd6D4e3bf75CA947D0C43d621Fef1`
